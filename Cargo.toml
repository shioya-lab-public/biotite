[package]
name = "biotite"
version = "0.2.0"
edition = "2021"
description = "A binary translator that lifts RISC-V to LLVM IR."
license = "MIT"

[dependencies]
clap = { version = "4.5.13", features = ["derive"] }
rayon = "1.10.0"
regex = "1.10.5"
