[package]
name = "riscv2llvm"
version = "0.2.0"
edition = "2021"
description = "A binary translator that lifts RISC-V to LLVM IR."
license = "MIT"

[dependencies]
clap = { version = "4.4.13", features = ["derive"] }
once_cell = "1.19.0"
rayon = "1.8.0"
regex = "1.10.2"
